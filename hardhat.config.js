require("@nomicfoundation/hardhat-toolbox");
require("dotenv").config(); // allows env variables to be read in hardhat

/** @type import('hardhat/config').HardhatUserConfig */
module.exports = {
  solidity: "0.8.18",
};
